{
    "version": 2,
    "builds": [
        {
            "src": "backend/app.js",
            "use": "@now/node-server"
        },
        {
            "src": "frontend/package.json",
            "use": "@now/static-build",
            "config": {
                "distDir": "frontend/build"
            }
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "backend/app.js"
        },
        {
            "src": "/(.*)",
            "dest": "frontend/build/$1"
        }
    ]
}